spring:
  datasource:
    url: jdbc:h2:./persons
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
  servlet:
    context-path: /api
  error:
    include-message: always

#management:
#  endpoints:
#    web.exposure.include: health, info
#  endpoint:
#    health:
#      enabled: true
#      show-details: always
logging:
  level:
    de.detim.njustus.person_db: debug
